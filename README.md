# PROG8860-w25-cicd

## Some Useful Commands for Lab1

Change the directory to lab-base

npm ci (To install all the packages)
npm run build --if-present (To build/compile the NodeJS Project)
npm test (To run Test cases)